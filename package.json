{
  "name": "quadrilateral",
  "description": "Monorepo for Quadrilateral code",
  "version": "1.0.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git://github.com/samuelsson/quadrilateral.git"
  },
  "license": "MIT",
  "workspaces": [
    "applications/*",
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "prepare": "husky install && lerna run prepare",
    "clean": "lerna clean",
    "cm": "npx git-cz",
    "test": "lerna run test",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.0.1",
    "@commitlint/config-conventional": "^12.0.1",
    "husky": "^5.1.2",
    "lerna": "^3.22.1"
  }
}
