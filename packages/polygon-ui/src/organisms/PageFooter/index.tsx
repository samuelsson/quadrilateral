import React from 'react';
import styles from './PageFooter.module.scss';

const PageFooter = (): JSX.Element => (
  <footer className={styles.PageFooter}>Hello from PageFooter</footer>
);

export default PageFooter;
