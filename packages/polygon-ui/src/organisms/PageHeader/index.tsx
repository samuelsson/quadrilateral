import React from 'react';
import styles from './PageHeader.module.scss';

const PageHeader = (): JSX.Element => (
  <header className={styles.PageHeader}>Hello from PageHeader</header>
);

export default PageHeader;
