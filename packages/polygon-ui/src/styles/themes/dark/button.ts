import { colors } from '../../abstracts';
import { Button } from '../types';

const button: Button = {
  primary: {
    background: colors.blueGray[100],
    foreground: colors.blueGray[800],
  },
};

export default button;
