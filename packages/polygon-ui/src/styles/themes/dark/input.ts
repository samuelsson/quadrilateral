import { Input } from '../types';
import { colors } from '../../abstracts';

const input: Input = {
  background: colors.blueGray[700],
  boxShadow: '',
  borderColor: colors.blueGray[900],
};

export default input;
