import { Input } from '../types';
import { colors } from '../../abstracts';

const input: Input = {
  background: colors.white,
  boxShadow: `0 4px 15px ${colors.gray[300]}`,
  borderColor: colors.gray[200],
};

export default input;
